-- 신규 정책: 관리자는 모든 컬렉션을 볼 수 있습니다.
-- has_role() 함수는 현재 사용자의 JWT에서 'admin' 역할을 확인합니다.
CREATE POLICY "Admins can view all collections."
ON public.collections FOR SELECT
TO authenticated
USING (public.has_role('admin'));
